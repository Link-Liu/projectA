[package]
name = "gateway"
version = "0.1.0"
edition = "2024"

[dependencies]
tokio = {"version" = "1", "feature" = ["full"] }
dashboard = { path = "../dashboard" } 
sensor_sim = { path = "../sensor_sim" } 

#need for component 3
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"